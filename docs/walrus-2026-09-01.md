# WaLRuS-DATA — 2026-09-01

Session scope: Discord wiring proven on the live gateway; ruling channel retargeted by Owner; ruling-channel boundary removed as decision debt; reasoning-loop cause researched and config applied; session ended with handoff for the (b)-enforcement vs Phase-8 decision.

---

## Wins

- Discord wiring (a) confirmed live: "demand a refund" → `[R7] order_refund — case …` posted via the real gateway (first real-platform round-trip).
- Ruling channel retargeted by Owner to #agentic-indoctrination (1478480447431376966); channel input check removed, recorded as decision debt.
- Reasoning-loop fix: grok research identified presence_penalty as Qwen's documented loop-breaker; applied alibaba/qwen3.8-max options {temperature 1.0, topP 0.95, presencePenalty 1.5} in opencode.json; JSON validated.
- Explained DashScope 400 InternalError.Algo.DataInspectionFailed as server-side output-moderation false positive, transient.

## Learnings

- Degenerate reasoning loops are mode collapse; temperature is the wrong knob. Qwen thinking set: temp 1.0 / top_p 0.95 / presence_penalty 1.5; frequency_penalty is not Qwen's lever; repetition_penalty secondary.
- DashScope DataInspectionFailed = output inspection false positive, likely tripped by repetitive output; unrelated to sampling params (bad keys would raise InvalidParameter).
- opencode per-model options are documented camelCase passthrough; the alibaba camelCase→snake_case wire mapping is the one unverified link.

## Risks

- Next session's first requests will test the config: unknown-key error → drop topP/presencePenalty; quality sag → presencePenalty 1.0–1.2; loop persists → 1.8–2.0.
- Bot persistence across reboots unresolved: currently WMI-detached; scheduled task `law-order-discord-bot` flaky (dies when task shell exits).
- Enforcement (b) absent → bot is advisory, not judicial. Phase 8 Mechanism 1 absent → any Discord measurement content-blind.

## Strategy / instructions for next session

1. Start per CLAUDE.md Session Start; verify config took effect on first requests (see Risks fallbacks).
2. Decide (cycle step 2), one atomic step: (b) enforcement actuation (makes rulings judicial: timeout/delete/AutoMod) vs Phase 8 Mechanism 1 (remove catch-all auto-resolution; nonsense test flips). Score both; if owner defers, Decide picks.
3. Fix bot persistence (service or robust task) before relying on it.
4. Session-end protocol each cycle: checkboxes, issue comment, WaLRuS.

## Decisions

- presence_penalty 1.5 accepted despite Qwen's precise-coding recommendation of 0.0, to break loops; watch code quality.
- Ruling channel = #agentic-indoctrination (Owner).
- Channel check removal = decision debt (revisit whether neutral placement is load-bearing).

## Alignment

- Constitution unchanged. Decision debt lives in IMPLEMENTATION.md. Handoff via IMPLEMENTATION.md note + issue #8 comment + this WaLRuS.

## Tradeoffs

- presencePenalty 1.5 vs identifier-heavy code quality — accepted, monitored.
- WMI-detached bot vs reboot persistence — accepted for now, flagged.

## Alternatives

- Temperature-only loop fix — rejected (didn't work).
- Disabling thinking — rejected (thinking is the value; loops are the tax).
